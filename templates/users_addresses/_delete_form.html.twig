<form method="post" action="{{ path('app_users_addresses_delete', {'user': users_address.user}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ users_address.user) }}">
    <button class="btn">Delete</button>
</form>
